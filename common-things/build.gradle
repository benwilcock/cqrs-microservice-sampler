buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
    }
}

jar {
    baseName = 'product-domain-events'
}


task wrapper(type: Wrapper) {
    gradleVersion = '2.3'
}

task image(){
    // Dummy task to allow global commands to work as intended
}
