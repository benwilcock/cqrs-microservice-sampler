<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:axon="http://www.axonframework.org/schema/core"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xmlns:axon-amqp="http://www.axonframework.org/schema/amqp"
       xmlns:mongo="http://www.springframework.org/schema/data/mongo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.axonframework.org/schema/core http://www.axonframework.org/schema/axon-core-2.0.xsd
                           http://www.springframework.org/schema/data/mongo http://www.springframework.org/schema/data/mongo/spring-mongo.xsd
                           http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit-1.0.xsd
                           http://www.axonframework.org/schema/amqp http://www.axonframework.org/schema/axon-amqp.xsd
                           ">

    <!-- Defaults to SimpleCommandBus. transaction-manager="rabbitTransactionManager" -->
    <axon:command-bus id="commandBus" />
    <axon:event-bus id="eventBus"/>

    <!-- event-bus="eventBus" event-store="eventStore" -->
    <axon:event-sourcing-repository id="todoRepository"
                                    aggregate-type="com.soagrowers.productview.aggregates.ProductAggregate"
                                    event-bus="eventBus"/>

    <axon:filesystem-event-store base-dir="events"/>

    <bean id="commandGatewayFactory" class="org.axonframework.commandhandling.gateway.CommandGatewayFactoryBean" >
        <property name="commandBus" ref="commandBus"/>
    </bean>

    <axon:aggregate-command-handler aggregate-type="com.soagrowers.productview.aggregates.ProductAggregate"
                                    repository="todoRepository"
                                    command-bus="commandBus"/>
    <!-- Scans for @CommandHandlers and @EventHandlers -->
    <axon:annotation-config />
    <!-- instantiates the given handlers -->
    <bean class="com.soagrowers.productview.handlers.EventLoggingHandler"/>


    <!--
    <bean id="eventStore" class="org.axonframework.eventstore.mongo.MongoEventStore">
        <constructor-arg ref="serializer"/>
        <constructor-arg ref="mongoTemplate"/>
    </bean>

    <mongo:mongo host="127.0.0.1" port="27017"/>

    <mongo:db-factory dbname="todo" />
    <bean id="mongoTemplate" class="org.axonframework.eventstore.mongo.DefaultMongoTemplate">
        <constructor-arg index="0" ref="mongo"/>
        <constructor-arg index="1" value="todo"/>
        <constructor-arg index="2" value="events"/>
        <constructor-arg index="3" value="snapshots"/>
        <constructor-arg index="4"><null/></constructor-arg>
        <constructor-arg index="5"><null/></constructor-arg>
    </bean>-->





</beans>